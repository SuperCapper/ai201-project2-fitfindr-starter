[pytest]
# Collect only the canonical suite under tests/. The top-level
# test_*.py files are manual, print-style scripts that reassign
# sys.stdout and make live LLM calls, so they are run by hand
# (e.g. `python test_suggest_outfit.py`), not under pytest.
testpaths = tests
